# Scripts

Reserved for helper scripts such as:

- `ingest.py`
- `query.py`
- `lint.py`
- `search.py`

At this stage the vault is scaffolded only. Scripts can be added once the first real sources are ingested.
